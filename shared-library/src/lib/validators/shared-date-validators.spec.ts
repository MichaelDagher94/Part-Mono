import { SharedDateValidators } from "./shared-date-validators";

describe("SharedValidators", () => {
  it("should create an instance", () => {
    expect(new SharedDateValidators()).toBeTruthy();
  });
});
