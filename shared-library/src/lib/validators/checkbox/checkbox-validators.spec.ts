import { CheckboxValidators } from './checkbox-validators';

describe('CheckboxValidators', () => {
  it('should create an instance', () => {
    expect(new CheckboxValidators()).toBeTruthy();
  });
});
